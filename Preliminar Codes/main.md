# Main Logic / Uart Communication / Interrupts

- Include Project Header files (ro call the functions we created in subcodes .c)

main.h - Central header generated by STM32CubeMX. Contains: GPIO pin definitions, Peripheral handles, System clock config prototypes, HAL includes.

<img width="448" height="155" alt="image" src="https://github.com/user-attachments/assets/e661b18b-5578-412d-9b7b-fcc972e13530" />

- Global Variables: added some local variables from the other codes in the main. This way we can check them live in debug mode.

<img width="227" height="113" alt="image" src="https://github.com/user-attachments/assets/065fecaa-b33c-4595-8af5-1f14bd35802b" />

- UART Communication Variables:

1. Single‑byte receive buffer: uint8_t rx_byte; // A temporary variable where the UART interrupt stores the latest received byte.

Note: UART hardware delivers one byte at a time, so the interrupt handler reads that byte into rx_byte. The interrupt cannot process full commands. It only grabs the byte and passes it to your parser.

<img width="440" height="38" alt="image" src="https://github.com/user-attachments/assets/8fb471c4-0970-48dc-9eae-5f975196bae2" />

2. Temporary character buffer (accumulates characters): char rx_buffer[64]; // A 64‑character buffer where you accumulate incoming characters until a full command is received. Size 64 because it is enough space for typical commands. Prevents overflow.

- <img width="298" height="23" alt="image" src="https://github.com/user-attachments/assets/af68afa1-7c3c-4912-8bdb-9ec09936dabb" />

- <img width="196" height="241" alt="image" src="https://github.com/user-attachments/assets/e76b6ad5-b922-42b4-babe-52291fcd1c56" />

3. Final command buffer: char command[64]; // When a newline \n arrives, the interrupt copies the full command from rx_buffer into command.

Note: Command becomes the stable, ready‑to‑process string. Meanwhile, rx_buffer can start receiving the next command.

- <img width="470" height="22" alt="image" src="https://github.com/user-attachments/assets/4cb26358-7ba2-4698-9582-54b24d7573f8" />

- <img width="188" height="237" alt="image" src="https://github.com/user-attachments/assets/b0ec8464-5291-4b3c-bc02-2d230c8790aa" />

4. Index inside rx_buffer: uint8_t rx_index = 0; // Tracks where the next incoming character should be stored inside rx_buffer.

- <img width="458" height="26" alt="image" src="https://github.com/user-attachments/assets/aa81d67d-2ede-4474-ae52-6de061867f23" />

- <img width="209" height="200" alt="image" src="https://github.com/user-attachments/assets/4a07b67f-af83-4474-a930-04b358565c09" />

5. Command‑ready flag: volatile uint8_t command_ready = 0; // A flag set by the UART interrupt: 0 → no complete command yet, 1 → a full command (ending in '\n') is ready.

Note: It is volatile because it is modified inside an interrupt. The main loop must always read the latest value. It is used in the while loop, everytime a command is ready the variable is cleaned (set to 0) and the function process command is called.

- <img width="233" height="50" alt="image" src="https://github.com/user-attachments/assets/8a0f3880-2003-4b65-a77c-36e2d991eb1d" />

- <img width="130" height="59" alt="image" src="https://github.com/user-attachments/assets/48fbd6ee-7244-4daa-b245-f96a91246f90" />

- Command Variables:

1. Access Control Variables:

<img width="295" height="85" alt="image" src="https://github.com/user-attachments/assets/32ede097-1af1-4703-9447-4bfe17cfa1a8" />

2. Lighting Control Variables:

<img width="233" height="118" alt="image" src="https://github.com/user-attachments/assets/41cba3ad-d783-454c-9d81-874d6e8fb99e" />

3. System Status Variables:

<img width="278" height="169" alt="image" src="https://github.com/user-attachments/assets/8d042475-92b0-47b6-a552-66744d700f6f" />

4. Periodic Task Timers:

<img width="262" height="124" alt="image" src="https://github.com/user-attachments/assets/24e36382-ffad-4858-a035-0ffffc56bb7c" />

- Private variables (HAL peripheral handles): These are HAL driver objects that represent your UART peripherals. Each handle stores: Baud rate, word length, stop bits, parity, hardware registers, DMA settings, Interrupt state. Every HAL function needs a handle to know which UART we’re talking to. Without these handles, the HAL cannot operate the UART hardware.

huart1 → configuration + state for USART1 (Used for ESP COM)
huart2 → configuration + state for USART2 (Used for Debug, serial monitor,...)

<img width="164" height="42" alt="image" src="https://github.com/user-attachments/assets/a05ce43e-3f8b-4ce6-8eb4-895316297b74" />

- Private function prototypes (HAL system functions):These are function prototypes for hardware initialization routines.

1. SystemClock_Config() - Configures: PLL, HCLK, PCLK1, PCLK2, Flash latency, Oscillator sourceThis sets the CPU frequency and all peripheral clocks.

2. MX_GPIO_Init() - Initializes all GPIO pins: Modes (input/output/AF/analog), Pull‑ups / pull‑downs, Output type, Speed.

3. MX_USART1_UART_Init() - Configures UART1: Baud rate, Word length, Stop bits, Parity, Mode (TX/RX), Interrupts.

4. MX_USART2_UART_Init() - Same as above, but for UART2.

<img width="218" height="48" alt="image" src="https://github.com/user-attachments/assets/04b39cf7-44b6-4ec7-86b9-c584721278a1" />

- USER CODE BEGIN PFP / END PFP: region where you declare your own function prototypes. CubeMX will not touch anything inside. These functions are part of the application logic, not HAL.

1. Process_Command() - Called when command_ready == 1. Parses the received command string. Executes actions like: Turn LED on/off, Move servo, Read temperature, Control fan.

2. Send_Status() - Sends a formatted status message back to the ESP8266. Includes: Temperature, Humidity, Motion state, Fan speed, LED state. 

<img width="175" height="74" alt="image" src="https://github.com/user-attachments/assets/f7c9a3c2-6ca7-4504-b588-0ac0e595778c" />

MAIN FUNCTION - int main(void)****

- Reset HAL and core system. Resets all HAL state and low‑level drivers. Initializes: Flash interface (wait states, access). SysTick timer (used for HAL_Delay() and timebase)

<img width="454" height="27" alt="image" src="https://github.com/user-attachments/assets/e8b8e745-8ad3-4ec2-bd4e-924ee0ccf04c" />

- System clock configuration: Sets up: Oscillator source (HSI, HSE, etc.), PLL multipliers/dividers, Core clock (HCLK), Peripheral clocks (PCLK1, PCLK2), This defines the CPU speed and all timing for peripherals (UART baud, timers, etc.). Your delay_us() and UART baud rates depend on this being correct.

<img width="194" height="26" alt="image" src="https://github.com/user-attachments/assets/ebb5f7dd-1008-4ffd-9c35-2700e332b8bf" />

- Initialize all configured peripherals (HAL/CubeMX): These functions are auto‑generated by CubeMX and use HAL internally.

1. MX_GPIO_Init(). Configures all GPIO pins: Modes (input, output, alternate, analog), Pull‑ups/pull‑downs, Output type (push‑pull/open‑drain), Speed. This is where PC1, PC2, PC3, etc. get their basic configuration.

2. MX_USART1_UART_Init(). Sets up UART1: Baud rate, Word length, Stop bits, Parity, TX/RX mode, Used for communication with the ESP8266 (in your case).

2. MX_USART2_UART_Init(). Same idea, but for UART2. Often used for debugging via USB‑serial or another external device.

<img width="249" height="50" alt="image" src="https://github.com/user-attachments/assets/3c62c215-0190-47af-a80d-ab991ad8d273" />

- Initialize your external modules (your drivers): 

<img width="440" height="179" alt="image" src="https://github.com/user-attachments/assets/44229211-060c-457e-b18b-725a59cb92ea" />

- Start UART interrupt reception: Enables interrupt‑based reception on UART1.

Parameters: 
1. &huart1 → which UART (USART1)
2. &rx_byte → where to store the received byte
3. 1 → receive one byte at a time

After this call: 
1. When a byte arrives from ESP8266, the UART interrupt fires.
2. HAL calls your HAL_UART_RxCpltCallback().
3. You append rx_byte into rx_buffer, update rx_index, and possibly set command_ready.

<img width="482" height="29" alt="image" src="https://github.com/user-attachments/assets/1c8ac0ea-862c-416b-8279-1060acc16615" />

WHILE****

- Process command from ESP8266: command_ready is set by the UART interrupt when a full command (ending in \n) is received. If it’s 1, you: Clear it back to 0 so the same command isn’t processed twice. Call Process_Command(), which reads the global command buffer and: Opens/closes door, Turns lights on/off or auto, Changes fan mode, etc.

<img width="265" height="70" alt="image" src="https://github.com/user-attachments/assets/82bcb48a-e57e-4c93-aa61-1ba77b1f2604" />

- Update DHT + fan PID control - This function typically: Reads temperature (via DHT_Read() inside or before). Updates the PID controller. Adjusts the fan PWM (PB6) based on temperature and setpoint. It’s called every loop, so the fan reacts continuously to temperature changes.

<img width="200" height="29" alt="image" src="https://github.com/user-attachments/assets/6f6c070e-6d92-464e-8d03-34cb27b2f091" />

- Check PIR every 200 ms: HAL_GetTick() returns milliseconds since startup. last_pir_time stores the last time you checked the PIR. Every 200 ms: You update last_pir_time. Call PIR_RawRead() to read the motion sensor (PC2). Store result in motion_detected (likely 0/1).

<img width="237" height="73" alt="image" src="https://github.com/user-attachments/assets/e33a7d36-54c9-44bd-960f-17ec82967a90" />

- LED logic: auto vs manual mode: 

<img width="217" height="266" alt="image" src="https://github.com/user-attachments/assets/758990c9-5b27-4e78-b3ff-20e1c1205ddd" />

- (Optional) Send status every 2 seconds: Currently commented out. Logic is similar to PIR timing: Every 2000 ms (2 s), call Send_Status(). This would periodically update the app/ESP8266 with system status.

Communication Notes:

- <img width="647" height="170" alt="image" src="https://github.com/user-attachments/assets/19813da0-029f-41ac-b96a-f91fdb725236" />

- <img width="121" height="218" alt="image" src="https://github.com/user-attachments/assets/c2e38b47-6a9a-4fe5-8376-55bc0e68276e" />

- <img width="126" height="158" alt="image" src="https://github.com/user-attachments/assets/00d123b3-f05f-4df1-9d16-3a8bb388aaa3" />

Function: void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart): this function is called AUTOMATICALLY by HAL.
Every time one byte is received through UART (because we used HAL_UART_Receive_IT), HAL triggers this callback. huart (1 or 2) indicates which UART generated the interrupt.

1. The next part that the interrupt comes from USART1. The MCU may have several UARTs (USART1, USART2, USART3…), this callback is shared, so we have to check if specifically USART1 receive the byte. If yes → process the byte, If not → ignore.

<img width="176" height="27" alt="image" src="https://github.com/user-attachments/assets/19b0cb97-3f21-41f6-bce5-e040e40a563e" />

2. The next part is to confirm if the received byte is a newline (‘\n’). This means “The command is complete.” Remember that the ESP8266 sends commands ending with \n.

<img width="130" height="28" alt="image" src="https://github.com/user-attachments/assets/f5edb74f-8699-400e-94b2-41be6cd6a402" />

3. Terminate the received string. Adds the null terminator \0. This converts the buffer into a valid C string. Example: L I G H T : O N \0.

After that, Copy the full command into the final buffer. Moves the command from the temporary buffer (rx_buffer). Into the final buffer (command). This allows the temporary buffer to start receiving the next command.

Then reset the buffer index. This prepares the buffer for the next command. The next byte will go into rx_buffer[0]

And signal the main loop that a command is ready. This flag tells the main program: “A full command has been received and is ready to be processed.”

<img width="157" height="56" alt="image" src="https://github.com/user-attachments/assets/72881894-221a-40bb-a051-00e87464a174" />

4.  If the byte is NOT ‘\n’ and NOT ‘\r’. \r (carriage return) is ignored. Which means other byte is part of that command.

<img width="168" height="25" alt="image" src="https://github.com/user-attachments/assets/64d600f3-12a5-49a8-8ed5-86b7f823e5b8" />

5. In case the past condition is true, next is to store the byte in the temporary buffer. This prevents buffer overflow, saves the received byte, increments rx_index for the next byte.

<img width="230" height="161" alt="image" src="https://github.com/user-attachments/assets/47fa3396-a292-455a-96b1-5610e3808aee" />

<img width="217" height="50" alt="image" src="https://github.com/user-attachments/assets/450a7819-0a73-4f3b-b4da-062f379094d9" />

6. Re‑enable UART interrupt reception. This is critical because reactivates reception of another byte. UART receives one byte at a time, without this line, UART would stop receiving data.

It creates a loop: 1 byte received → callback → re‑enable reception → wait for next byte

<img width="305" height="38" alt="image" src="https://github.com/user-attachments/assets/e17a3b58-cf01-49ca-a046-ed08022335d9" />

Function: void Process_Command(void): This is the function that processes the command received from UART (ESP8266 → STM32).

1. Check if the command starts with "PASS:" strncmp compares the first 5 characters of command with "PASS:". If the command begins with "PASS:", then this is a password authentication command. Example command: PASS:1234

<img width="228" height="16" alt="image" src="https://github.com/user-attachments/assets/2f0b09eb-70b0-40f5-a52f-8296f7f1a6da" />

2. Extract the password sent by the user. Moves the pointer 5 characters forward. Skips "PASS:" and points directly to the password. Example: command = "PASS:1234". received_password = "1234".

<img width="217" height="16" alt="image" src="https://github.com/user-attachments/assets/93fab396-b60b-4028-ac4d-68404c54b5f4" />


3. Compare received password with the correct one. strcmp returns 0 if both strings are identical. If the password is correct → enter the “ACCESS GRANTED” block.

<img width="305" height="24" alt="image" src="https://github.com/user-attachments/assets/ff9b7b70-b48b-46a2-9bb7-d21b7b5a742d" />

4. ACCESS GRANTED block: Reset wrong attempts counter. Mark door as open. Physically move servo to open the door. Disable alarm.

<img width="119" height="47" alt="image" src="https://github.com/user-attachments/assets/16aca784-501e-4644-af88-3ecb2b021902" />

5. HAL_UART_Transmit — ACCESS GRANTED

&huart1: Selects UART1 as the transmitter. 

(uint8_t*)"ACK:ACCESS_GRANTED\n": This is the message sent back to the ESP8266. It is a string literal, cast to uint8_t* because HAL expects bytes. 

strlen("ACK:ACCESS_GRANTED\n"): Calculates how many bytes to send. This ensures the exact length is transmitted.

100: Timeout in milliseconds. If UART cannot send the message within 100 ms, the function returns an error.

Behavior: Blocking transmit → the CPU waits until all bytes are sent. Sends the message immediately.







